import { Meta, Controls, Primary, Canvas } from "@storybook/blocks";
import * as DialogStories from "./Dialog.stories";
import * as DialogList from "./DialogList.stories";
import * as DialogListItem from "./DialogListItem.stories";
import * as DialogMetadata from "./DialogMetadata.stories";

<Meta of={DialogList} />

# Dialog list

The DialogList component takes a list of dialogs as items and groups them, using the groups prop and the items groupId.

## Grouped by date

<Canvas of={DialogList.GroupedByDate} />
