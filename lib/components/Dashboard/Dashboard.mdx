import { Meta, Controls, Primary, Canvas } from "@storybook/blocks";
import * as DashboardHeaderStories from "./DashboardHeader.stories";

<Meta title="Page/Dashboard" />

# Dashboard pages

Build dashboards using `DashboardHeader`, `Grid`, `DashboardCard`, and general purpuse components like `Section`, `Divider`, `Heading` and `Typography`.

## Person dashboard

<Canvas of={DashboardHeaderStories.Page} />

## Company dashboard

<Canvas of={DashboardHeaderStories.Company} />
