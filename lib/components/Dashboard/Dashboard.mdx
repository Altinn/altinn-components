import { Meta, Controls, Primary, Canvas } from "@storybook/addon-docs/blocks";
import * as DashboardStories from "./Dashboard.stories";

<Meta title="Page/Dashboard" />

# Dashboard pages

Build dashboards using `DashboardHeader`, `Grid`, `DashboardCard`, and general purpuse components like `Section`, `Divider`, `Heading` and `Typography`.

## Person dashboard

<Canvas of={DashboardStories.PersonDashboard} />

## Company dashboard

<Canvas of={DashboardStories.CompanyDashboard} />
