import { Meta, Controls, Primary, Canvas } from "@storybook/blocks";
import * as Patterns from "./Patterns.stories";
import * as PageHeader from "./PageHeader.stories";

<Meta title="Page" />

# Building pages

Page components includes a set of patterns and components that can help you build pages with consistency in spacing, padding and so on.

## Using PageHeader

Page header with an icon representing a service.

<Canvas of={PageHeader.Icon} />

User card.

<Canvas of={PageHeader.UserCard} />

Company card with tabs.

<Canvas of={PageHeader.CardAndTabs} />
