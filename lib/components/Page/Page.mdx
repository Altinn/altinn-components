import { Meta, Controls, Primary, Canvas } from "@storybook/blocks";
import * as PageHeader from "./PageHeader.stories";
import * as DashboardHeader from "./DashboardHeader.stories";

<Meta title="Page" />

# Building pages

Page components includes a set of patterns and components that can help you build pages with consistency in spacing, padding and so on.

## Dashboard components

Dashboard header representing a person or a company.

<Canvas of={DashboardHeader.Person} />

<Canvas of={DashboardHeader.Company} />
