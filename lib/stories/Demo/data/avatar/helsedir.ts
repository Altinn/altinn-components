import type { AvatarProps } from '../../components';
import logo from './helsedir-logo.jpg';

export const helsedir: AvatarProps = {
  type: 'company',
  name: 'Helsedirektoratet',
  imageUrl: logo,
};
