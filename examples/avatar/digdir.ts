import type { AvatarProps } from '../../lib/components';
import logo from './digdir-logo.png';

export const digdir: AvatarProps = {
  type: 'company',
  name: 'Digitaliseringsdirektoratet',
  imageUrl: logo,
};
