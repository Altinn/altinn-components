import { globalMenu, localeSwitcher } from '../';
import type { GlobalHeaderProps } from '../../lib';

export const header: GlobalHeaderProps = {
  globalMenu: globalMenu,
  locale: localeSwitcher,
};
